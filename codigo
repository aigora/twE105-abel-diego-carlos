#include<stdio.h>

void main()
{
	int a, b, c, d;
	char id[20];
	
	printf("\n\t > Bienvenido a TRANSFUTURE\n\n");
	printf("\n\tIniciar sesion(1)\t Registrarse(2): ");
	scanf("%i", &a);
	
	switch(a)
	{
		case 1:
			printf("Usuario:\t");
			scanf("%s",id);
		case 2:
		printf("\n > Identificate:\n\tSoy cliente(1)\t Soy empleado(2)\n");
		scanf("%i", &b);
		
		if(b == 1)
		{
			usuario usuario1;
			printf("Introduce tu nick de usuario");
			scanf(" %[^\n]",usuario1.id);
			printf("\n Introduce tu nombre:");
			scanf (" %[^\n]",usuario1.nombre);
			printf("\n Introduce tus apellidos");
			scanf(" %[^\n]",usuario1.apellidos);
			printf ("Introduce tu contraseña (debe ser menor de 20 caracteres): ");
			scanf ("[^\n]",usuario1.contraseña);
		}
		break;
		default:
	}
	
	return 0;
}
